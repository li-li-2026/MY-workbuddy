<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,user-scalable=no,viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="apple-mobile-web-app-title" content="李李工作台">
<meta name="theme-color" content="#4c6e4b">
<meta name="mobile-web-app-capable" content="yes">
<link rel="manifest" href='data:application/json,{"name":"李李的工作台","short_name":"李李工作台","start_url":".","display":"standalone","orientation":"portrait","background_color":"#f4f6f4","theme_color":"#4c6e4b","icons":[{"src":"data:image/svg+xml,%3Csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 512 512%27%3E%3Crect width=%27512%27 height=%27512%27 rx=%27100%27 fill=%27%234c6e4b%27/%3E%3Ctext x=%27256%27 y=%27256%27 font-family=%27Helvetica,Arial,sans-serif%27 font-size=%27280%27 font-weight=%27bold%27 fill=%27%23ffffff%27 text-anchor=%27middle%27 dominant-baseline=%27central%27%3E文%3C/text%3E%3C/svg%3E","sizes":"192x192","type":"image/png","purpose":"any maskable"}]}'>
<link rel="apple-touch-icon" href="data:image/svg+xml,%3Csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 512 512%27%3E%3Crect width=%27512%27 height=%27512%27 rx=%27100%27 fill=%27%234c6e4b%27/%3E%3Ctext x=%27256%27 y=%27256%27 font-family=%27Helvetica,Arial,sans-serif%27 font-size=%27280%27 font-weight=%27bold%27 fill=%27%23ffffff%27 text-anchor=%27middle%27 dominant-baseline=%27central%27%3E文%3C/text%3E%3C/svg%3E">
<link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 512 512%27%3E%3Crect width=%27512%27 height=%27512%27 rx=%27100%27 fill=%27%234c6e4b%27/%3E%3Ctext x=%27256%27 y=%27256%27 font-family=%27Helvetica,Arial,sans-serif%27 font-size=%27280%27 font-weight=%27bold%27 fill=%27%23ffffff%27 text-anchor=%27middle%27 dominant-baseline=%27central%27%3E文%3C/text%3E%3C/svg%3E">
<title>李李工作台</title>
<style>
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
:root{--bg:#ededed;--card:#fff;--text:#1a1a1a;--muted:#888;--primary:#07c160;--primary-light:#e8f8ef;--primary-dark:#06ad56;--accent:#5e7ce0;--warn:#ff9c19;--danger:#fa5151;--border:#e6e6e6;--radius:12px;--safe-top:env(safe-area-inset-top,0px);--safe-bottom:env(safe-area-inset-bottom,0px)}
html,body{height:100%;overflow:hidden}
body{font-family:-apple-system,BlinkMacSystemFont,"PingFang SC","Microsoft YaHei",sans-serif;background:var(--bg);color:var(--text);-webkit-user-select:none;user-select:none}
.app{max-width:520px;margin:0 auto;height:100vh;display:flex;flex-direction:column;position:relative}
.topbar{background:var(--card);padding:calc(12px + var(--safe-top)) 16px 12px;border-bottom:1px solid var(--border);position:relative;z-index:5;flex-shrink:0}
.topbar h1{font-size:19px;font-weight:800}
.topbar p{font-size:12px;color:var(--muted);margin-top:2px}
.content{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:12px 0 80px}
.menu-group{background:var(--card);border-radius:var(--radius);overflow:hidden;margin:0 12px 8px}
.menu-item{display:flex;align-items:center;gap:12px;padding:14px 16px;cursor:pointer;border-bottom:1px solid var(--border);position:relative;transition:background .1s}
.menu-item:last-child{border-bottom:none}
.menu-item:active{background:#f5f5f5}
.menu-icon{width:36px;height:36px;border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:18px;flex-shrink:0}
.menu-info{flex:1;min-width:0}
.menu-info .name{font-size:15px;font-weight:600}
.menu-info .desc{font-size:12px;color:var(--muted);margin-top:1px}
.menu-arrow{color:#c8c8c8;font-size:16px}
.menu-badge{background:var(--danger);color:#fff;font-size:10px;padding:1px 6px;border-radius:999px;font-weight:700}
.section-title{font-size:13px;font-weight:700;color:var(--muted);margin:12px 16px 8px}
.detail{display:none;position:absolute;inset:0;z-index:20;background:var(--bg);flex-direction:column}
.detail.active{display:flex}
.detail-header{background:var(--card);padding:calc(10px + var(--safe-top)) 12px 10px;border-bottom:1px solid var(--border);display:flex;align-items:center;gap:8px;flex-shrink:0}
.back{background:none;border:none;font-size:16px;color:var(--primary);cursor:pointer;font-weight:600;display:flex;align-items:center;gap:2px}
.detail-header h2{font-size:17px;font-weight:800}
.detail-body{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:12px 16px calc(20px + var(--safe-bottom))}
.card{background:var(--card);border-radius:var(--radius);padding:16px;margin-bottom:10px}
.card h3{font-size:15px;font-weight:700;margin-bottom:12px;display:flex;align-items:center;gap:6px}
.btn{border:none;border-radius:10px;padding:9px 14px;font-size:14px;font-weight:600;cursor:pointer;display:inline-flex;align-items:center;gap:5px}
.btn-primary{background:var(--primary);color:#fff}
.btn-primary:active{background:var(--primary-dark)}
.btn-soft{background:var(--primary-light);color:var(--primary-dark)}
.btn-gray{background:#f0f0f0;color:var(--text)}
.btn-danger{background:#ffeded;color:var(--danger)}
.toolbar{display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap}
input,textarea,select{border:1px solid var(--border);border-radius:10px;padding:9px 11px;font-size:14px;outline:none;font-family:inherit;width:100%}
input:focus,textarea:focus{border-color:var(--primary)}
.form-row{display:flex;gap:8px;margin-bottom:8px}
.form-row input,.form-row select{flex:1}
.list{list-style:none}
.list li{display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid var(--border)}
.list li:last-child{border-bottom:none}
.list li input[type=checkbox]{width:20px;height:20px;accent-color:var(--primary);flex-shrink:0}
.list li .text{flex:1;font-size:14px;line-height:1.4}
.list li .text.done{text-decoration:line-through;color:var(--muted)}
.badge{display:inline-block;padding:2px 7px;border-radius:999px;font-size:11px;font-weight:700}
.b-green{background:#d1fae5;color:#065f46}.b-blue{background:#dbeafe;color:#1e40af}
.b-amber{background:#fef3c7;color:#92400e}.b-red{background:#fee2e2;color:#991b1b}
.idea{background:#f9fafb;border:1px solid var(--border);border-radius:10px;padding:12px;margin-bottom:8px}
.idea .t{font-weight:600;margin-bottom:4px;font-size:14px;line-height:1.4}
.idea .m{font-size:12px;color:var(--muted)}
.idea .actions{margin-top:8px;display:flex;gap:6px}
.progress{background:#e5e7eb;border-radius:999px;height:7px;overflow:hidden;margin-top:8px}
.progress-bar{background:var(--primary);height:100%;transition:width .3s}
.loading{text-align:center;color:var(--muted);padding:16px;font-size:14px}
.hot-item{display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid var(--border)}
.hot-item:last-child{border-bottom:none}
.hot-rank{width:22px;text-align:center;font-weight:800;font-size:15px;color:var(--muted)}
.hot-rank.top{color:var(--danger)}
.hot-title{flex:1;font-size:14px;line-height:1.4}
.hot-hot{font-size:11px;color:var(--warn);white-space:nowrap}
table{width:100%;border-collapse:collapse;font-size:13px}
th,td{text-align:left;padding:8px 6px;border-bottom:1px solid var(--border)}
th{color:var(--muted);font-size:12px}
.empty{text-align:center;color:var(--muted);padding:20px;font-size:13px}
footer{text-align:center;color:var(--muted);font-size:11px;padding:16px}
.tabbar{display:flex;background:var(--card);border-top:1px solid var(--border);padding:4px 0 calc(4px + var(--safe-bottom));flex-shrink:0}
.tabbar .tab{flex:1;text-align:center;font-size:11px;color:var(--muted);cursor:pointer;padding:4px}
.tab.active{color:var(--primary);font-weight:700}
.tab .icon{font-size:20px;display:block;margin-bottom:2px}
.fade-in{animation:fadeIn .2s ease}
@keyframes fadeIn{from{opacity:0;transform:translateX(20px)}to{opacity:1;transform:none}}
</style>
</head>
<body>
<div class="app">
<div class="topbar"><h1>创作工作台</h1><p>完成一项打勾，进度一目了然</p></div>
<div class="content" id="home-content">
<div class="section-title">📌 今日任务</div>
<div class="menu-group" id="today-group"></div>
<div class="section-title">📚 学习成长</div>
<div class="menu-group" id="learn-group"></div>
<div class="section-title">💡 内容创作</div>
<div class="menu-group" id="create-group"></div>
<div class="section-title">🧘 生活记录</div>
<div class="menu-group" id="life-group"></div>
<footer>WorkBuddy · 数据保存在本机</footer>
</div>
<div class="tabbar">
<div class="tab active" onclick="goHome()"><span class="icon">🏠</span>工作台</div>
<div class="tab" onclick="goStats()"><span class="icon">📊</span>统计</div>
<div class="tab" onclick="goSettings()"><span class="icon">⚙️</span>设置</div>
</div>
<div class="detail" id="detail-view">
<div class="detail-header"><button class="back" onclick="closeDetail()">‹ 返回</button><h2 id="detail-title">详情</h2></div>
<div class="detail-body" id="detail-body"></div>
</div>
</div>
<script>
const $=s=>document.querySelector(s);
const $$=s=>[...document.querySelectorAll(s)];
const LS=localStorage;
const today=()=>new Date().toLocaleDateString('zh-CN');
function save(k,v){LS.setItem('wb_'+k,JSON.stringify(v))}
function load(k,d){try{return JSON.parse(LS.getItem('wb_'+k))??d}catch{return d}}
let habits=load('habits',[{id:1,name:'舞蹈练习 30 分钟',done:false,cat:'hiphop'},{id:2,name:'吉他爬格子 20 分钟',done:false,cat:'guitar'},{id:3,name:'体态矫正训练',done:false,cat:'posture'},{id:4,name:'阅读/学习 30 分钟',done:false,cat:'learn'}]);
let topics=load('topics',[]);
let reviews=load('reviews',[]);
let danceNotes=load('danceNotes',[]);
let songs=load('songs',[]);
let gears=load('gears',[]);
let postureLogs=load('postureLogs',[]);
let moods=load('moods',[]);
let avoids=load('avoids',[]);
let products=load('products',[]);
const menuItems=[
{id:'checkin',name:'每日打卡计划',desc:'舞蹈·吉他·体态·学习',icon:'✅',color:'#07c160',group:'today',badge:0},
{id:'video',name:'短视频选题灵感',desc:'自动抓取热点 · 文案复盘',icon:'🎬',color:'#5e7ce0',group:'create',badge:1},
{id:'hiphop',name:'Hiphop 舞蹈自学',desc:'周训练计划 · 纠错笔记',icon:'🕺',color:'#fa5151',group:'learn',badge:0},
{id:'guitar',name:'电吉他自学进度',desc:'入门知识 · 乐谱 · 配件',icon:'🎸',color:'#ff9c19',group:'learn',badge:0},
{id:'posture',name:'体态矫正记录',desc:'高低肩·腰酸矫正打卡',icon:'🧘',color:'#07c160',group:'life',badge:0},
{id:'mood',name:'每日金融小知识',desc:'每天一个股票/金融知识·配视频',icon:'💰',color:'#5e7ce0',group:'life',badge:0},
{id:'fitness',name:'健身训练',desc:'选部位生成动作·配套视频',icon:'💪',color:'#fa5151',group:'today',badge:0},
{id:'posturefix',name:'体态矫正专区',desc:'大小脸·脊柱侧弯·O型腿·足弓',icon:'🦴',color:'#5e7ce0',group:'today',badge:0},
{id:'english',name:'英语口语练习',desc:'三个月开口说·跟读+对话',icon:'🗣️',color:'#5e7ce0',group:'learn',badge:0},
{id:'operate',name:'运营学习',desc:'0基础入门·新媒体运营实战',icon:'📈',color:'#ff9c19',group:'learn',badge:0}
];
function renderMenu(){
const todayHtml=menuItems.filter(m=>m.group==='today').map(renderMenuItem).join('');
const learnHtml=menuItems.filter(m=>m.group==='learn').map(renderMenuItem).join('');
const createHtml=menuItems.filter(m=>m.group==='create').map(renderMenuItem).join('');
const lifeHtml=menuItems.filter(m=>m.group==='life').map(renderMenuItem).join('');
$('#today-group').innerHTML=todayHtml;
$('#learn-group').innerHTML=learnHtml;
$('#create-group').innerHTML=createHtml;
$('#life-group').innerHTML=lifeHtml;
}
function renderMenuItem(m){
let badge='';
if(m.id==='checkin'){const c=habits.filter(h=>!h.done).length;if(c)badge=`<span class="menu-badge">${c}</span>`}
return `<div class="menu-item" onclick="openDetail('${m.id}')"><div class="menu-icon" style="background:${m.color}22">${m.icon}</div><div class="menu-info"><div class="name">${m.name}</div><div class="desc">${m.desc}</div></div>${badge}<span class="menu-arrow">›</span></div>`;
}
function openDetail(id){
const m=menuItems.find(x=>x.id===id);
$('#detail-title').textContent=m.name;
$('#detail-body').innerHTML=detailRenderers[id]();
$('#detail-view').classList.add('active');
$('#detail-view').classList.add('fade-in');
setTimeout(()=>$('#detail-view').classList.remove('fade-in'),250);
if(detailInits[id])detailInits[id]();
}
function closeDetail(){$('#detail-view').classList.remove('active')}
const detailRenderers={
checkin:()=>`<div class="card"><h3>📋 每日打卡</h3><div class="form-row"><input type="text" id="new-habit" placeholder="添加打卡项，如：练吉他 20 分钟"><button class="btn btn-primary" onclick="addHabit()">添加</button></div><ul class="list" id="habit-list"></ul><div class="progress"><div class="progress-bar" id="habit-progress" style="width:0%"></div></div></div><button class="btn btn-gray" style="width:100%" onclick="resetDaily()">🔄 重置今日打卡</button>`,
video:()=>`<div class="card"><h3>🔥 实时热搜抓取</h3><div class="toolbar"><button class="btn btn-primary" onclick="fetchHot('douyin')">抓取抖音热点</button><button class="btn btn-soft" onclick="fetchHot('bilihot')">抓取B站热点</button></div><div id="hot-list"><div class="empty">点击上方按钮抓取最新热点</div></div></div><div class="card"><h3>💡 我的选题灵感</h3><div class="form-row"><input type="text" id="v-idea" placeholder="输入选题想法"><button class="btn btn-primary" onclick="addTopic()">收藏</button></div><div id="topic-list"></div></div><div class="card"><h3>📝 文案复盘</h3><div class="form-row"><input type="text" id="v-title" placeholder="视频标题"><select id="v-type"><option>干货教程</option><option>生活vlog</option><option>挑战/变装</option><option>情绪共鸣</option></select><button class="btn btn-primary" onclick="addReview()">保存</button></div><textarea id="v-notes" rows="2" placeholder="数据表现、可复用点、下次改进..."></textarea><ul class="list" id="review-list" style="margin-top:8px"></ul></div>`,
hiphop:()=>`<div class="card"><h3>📅 本周训练计划</h3><div class="toolbar"><button class="btn btn-primary" onclick="genDancePlan()">生成训练计划</button></div><div id="dance-plan"></div></div><div class="card"><h3>🩺 动作纠错笔记</h3><div class="form-row"><input type="text" id="dance-note-input" placeholder="如：做 Wave 时肩膀太紧"><button class="btn btn-primary" onclick="addDanceNote()">添加</button></div><ul class="list" id="dance-notes"></ul></div>`,
guitar:()=>`<div class="card"><h3>📚 入门知识点</h3><div class="toolbar"><button class="btn btn-primary" onclick="showGuitarBasics()">整理入门知识</button></div><div id="guitar-basics"></div></div><div class="card"><h3>🎼 乐谱 & 练习记录</h3><div class="form-row"><input type="text" id="g-song" placeholder="曲目名"><select id="g-status"><option>学习中</option><option>已会</option><option>复习</option></select><button class="btn btn-primary" onclick="addSong()">添加</button></div><ul class="list" id="guitar-songs"></ul></div><div class="card"><h3>🔌 配件功课</h3><div class="form-row"><input type="text" id="g-gear" placeholder="效果器/音箱/线材"><button class="btn btn-soft" onclick="addGear()">添加</button></div><ul class="list" id="gear-notes"></ul></div>`,
posture:()=>`<div class="card"><h3>🧘 矫正动作清单</h3><div class="toolbar"><button class="btn btn-primary" onclick="genPosturePlan()">生成矫正清单</button><button class="btn btn-soft" onclick="addPostureLog()">完成打卡</button></div><div id="posture-plan"></div></div><div class="card"><h3>📊 打卡记录</h3><ul class="list" id="posture-logs"></ul></div>`,
mood:()=>`<div class="card"><h3>💰 今日金融小知识</h3><div style="font-size:13px;color:#888;margin-bottom:10px;line-height:1.5">每天学一个股票/金融知识，点击主题查看<strong style="color:#5e7ce0">B站/抖音教学视频</strong>。</div><div class="toolbar"><button class="btn btn-primary" onclick="genFinPlan('stock')">股票基础</button><button class="btn btn-soft" onclick="genFinPlan('fund')">基金理财</button><button class="btn btn-soft" onclick="genFinPlan('economy')">宏观经济</button><button class="btn btn-soft" onclick="genFinPlan('skill')">实战技巧</button></div><div id="fin-plan"><div class="empty">点上方按钮，生成今日知识点 + 视频</div></div></div><div class="card"><h3>📚 快捷视频入口</h3><div id="fin-videos"></div></div><div class="card"><h3>✏️ 学习打卡</h3><div class="form-row"><input type="text" id="fin-log-input" placeholder="如：学了K线图基础+笔记"><button class="btn btn-primary" onclick="addFinLog()">记录</button></div><ul class="list" id="fin-logs"></ul></div>`,
fitness:()=>`<div class="card"><h3>💪 选择训练部位</h3><div class="toolbar" id="fit-parts"><button class="btn btn-primary" onclick="genFitPlan('胸')">胸肌</button><button class="btn btn-soft" onclick="genFitPlan('背')">背部</button><button class="btn btn-soft" onclick="genFitPlan('腿')">腿部</button><button class="btn btn-soft" onclick="genFitPlan('肩')">肩部</button><button class="btn btn-soft" onclick="genFitPlan('核心')">核心</button><button class="btn btn-soft" onclick="genFitPlan('全身燃脂')">全身燃脂</button></div><div id="fit-plan"><div class="empty">点上方按钮，自动生成动作 + 教学视频</div></div></div><div class="card"><h3>🎯 我的训练记录</h3><div class="form-row"><input type="text" id="fit-log-input" placeholder="如：卧推 40kg×10×3组"><button class="btn btn-primary" onclick="addFitLog()">记录</button></div><ul class="list" id="fit-logs"></ul></div>`,
english:()=>`<div class="card"><h3>🗣️ 三个月口语突破计划</h3><div style="font-size:13px;color:#888;margin-bottom:10px;line-height:1.5">点击月份查看每日练习任务，每个任务都配了<strong style="color:#5e7ce0">B站/抖音教学视频</strong>，跟着练就行。</div><div class="toolbar"><button class="btn btn-primary" onclick="genEngPlan(1)">第1月·打基础</button><button class="btn btn-soft" onclick="genEngPlan(2)">第2月·练表达</button><button class="btn btn-soft" onclick="genEngPlan(3)">第3月·实战</button></div><div id="eng-plan"><div class="empty">点上方按钮，生成当月练习计划 + 视频</div></div></div><div class="card"><h3>📚 快捷视频入口</h3><div id="eng-videos"></div></div><div class="card"><h3>✏️ 口语练习打卡</h3><div class="form-row"><input type="text" id="eng-log-input" placeholder="如：跟读VOA 15分钟 + 录音对比"><button class="btn btn-primary" onclick="addEngLog()">记录</button></div><ul class="list" id="eng-logs"></ul></div>`,
operate:()=>`<div class="card"><h3>📈 0基础运营入门路线</h3><div style="font-size:13px;color:#888;margin-bottom:10px;line-height:1.5">从0开始学运营，按阶段点击查看学习任务，每个任务都配了<strong style="color:#ff9c19">B站/抖音教学视频</strong>。</div><div class="toolbar"><button class="btn btn-primary" onclick="genOpPlan(1)">阶段1·认知</button><button class="btn btn-soft" onclick="genOpPlan(2)">阶段2·实操</button><button class="btn btn-soft" onclick="genOpPlan(3)">阶段3·进阶</button></div><div id="op-plan"><div class="empty">点上方按钮，生成学习任务 + 视频</div></div></div><div class="card"><h3>📚 运营快捷视频</h3><div id="op-videos"></div></div><div class="card"><h3>✏️ 学习打卡</h3><div class="form-row"><input type="text" id="op-log-input" placeholder="如：学了小红书选题技巧 + 笔记"><button class="btn btn-primary" onclick="addOpLog()">记录</button></div><ul class="list" id="op-logs"></ul></div>`,
posturefix:()=>`<div class="card"><h3>🦴 体态矫正专区</h3><div style="font-size:13px;color:#888;margin-bottom:10px;line-height:1.5">选择你的体态问题，生成矫正动作 + <strong style="color:#5e7ce0">B站/抖音教学视频</strong>。</div><div class="toolbar"><button class="btn btn-primary" onclick="genPfPlan('face')">大小脸/脸型</button><button class="btn btn-soft" onclick="genPfPlan('spine')">脊柱侧弯</button><button class="btn btn-soft" onclick="genPfPlan('leg')">O型腿</button><button class="btn btn-soft" onclick="genPfPlan('foot')">足弓塌陷</button><button class="btn btn-soft" onclick="genPfPlan('calf')">小腿外翻</button><button class="btn btn-soft" onclick="genPfPlan('chin')">下巴后缩</button></div><div id="pf-plan"><div class="empty">点上方按钮，生成矫正动作 + 视频</div></div></div><div class="card"><h3>⚠️ 重要提醒</h3><div style="font-size:13px;color:#888;line-height:1.6"><p>• <b>大小脸/大小眼/颧骨突出/脸型不流畅</b>：多为骨骼结构问题，训练改善有限，严重建议咨询正颌或正畸医生</p><p>• <b>脊柱侧弯</b>：Cobb角＞40度建议就医，轻度可通过施罗特体操改善</p><p>• <b>下巴后缩</b>：可能是牙齿咬合问题，建议看正畸科</p><p>• 矫正训练需<b>每天坚持</b>，至少3个月才有效果</p></div></div><div class="card"><h3>✏️ 矫正打卡</h3><div class="form-row"><input type="text" id="pf-log-input" placeholder="如：施罗特体操20分钟+足弓训练"><button class="btn btn-primary" onclick="addPfLog()">记录</button></div><ul class="list" id="pf-logs"></ul></div>`
};
const detailInits={
checkin:()=>{renderCheckin()},
video:()=>{renderTopics();renderReviews()},
hiphop:()=>{renderDanceNotes()},
guitar:()=>{renderSongs();renderGears()},
posture:()=>{renderPostureLogs()},
mood:()=>{renderFinLogs();renderFinVideos()},
fitness:()=>{renderFitLogs()},
english:()=>{renderEngLogs();renderEngVideos()},
operate:()=>{renderOpLogs();renderOpVideos()},
posturefix:()=>{renderPfLogs()}
};
function renderCheckin(){
const l=$('#habit-list');if(!l)return;l.innerHTML='';
habits.forEach((h,i)=>{
const li=document.createElement('li');
li.innerHTML=`<input type="checkbox" ${h.done?'checked':''} onchange="toggleHabit(${i})"><div class="text ${h.done?'done':''}">${h.name}</div><button class="btn btn-danger" onclick="delHabit(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
const pct=habits.length?Math.round(habits.filter(h=>h.done).length/habits.length*100):0;
$('#habit-progress').style.width=pct+'%';
}
function toggleHabit(i){
habits[i].done=!habits[i].done;save('habits',habits);
if(habits[i].done){let s=load('streak',0);const last=load('lastCheck','');if(last!==today()){save('streak',s+1);save('lastCheck',today())}}
renderCheckin();renderMenu();
}
function addHabit(){const v=$('#new-habit').value.trim();if(!v)return;habits.push({id:Date.now(),name:v,done:false,cat:'other'});save('habits',habits);$('#new-habit').value='';renderCheckin();renderMenu()}
function delHabit(i){habits.splice(i,1);save('habits',habits);renderCheckin();renderMenu()}
function resetDaily(){habits.forEach(h=>h.done=false);save('habits',habits);renderCheckin();renderMenu()}
async function fetchHot(type){
const el=$('#hot-list');el.innerHTML='<div class="loading">⏳ 正在抓取热点...</div>';
try{
const res=await fetch(`https://api.zxki.cn/api/jhrs?type=${type}`);
const d=await res.json();
if(!d.data)throw 0;
el.innerHTML=d.data.slice(0,15).map((h,i)=>{
const title=h.title||h.name||'';
const hot=h.hot||'';
return `<div class="hot-item"><div class="hot-rank ${i<3?'top':''}">${i+1}</div><div class="hot-title">${title}</div>${hot?`<div class="hot-hot">${hot}</div>`:''}</div>`;
}).join('');
d.data.slice(0,3).forEach(h=>{
const t=h.title||h.name;
if(t&&!topics.find(x=>x.title===t))topics.push({title:t,src:type==='douyin'?'抖音':'B站',date:today()});
});
save('topics',topics);renderTopics();
}catch(e){el.innerHTML='<div class="empty">⚠️ 抓取失败，请稍后重试</div>';}
}
function renderTopics(){
const el=$('#topic-list');if(!el)return;
el.innerHTML=topics.length?topics.map((t,i)=>`<div class="idea"><div class="t">${t.title}</div><div class="m">${t.src} · ${t.date}</div></div>`).join(''):'<div class="empty">暂无选题灵感</div>';
}
function addTopic(){const v=$('#v-idea').value.trim();if(!v)return;topics.push({title:v,src:'手动',date:today()});save('topics',topics);$('#v-idea').value='';renderTopics()}
function renderReviews(){
const l=$('#review-list');if(!l)return;l.innerHTML='';
reviews.forEach((r,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text"><b>${r.title}</b> <span class="badge b-blue">${r.type}</span><br><small style="color:#888">${r.notes}</small></div><button class="btn btn-danger" onclick="delReview(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addReview(){const t=$('#v-title').value.trim(),ty=$('#v-type').value,n=$('#v-notes').value.trim();if(!t)return;reviews.unshift({title:t,type:ty,notes:n});save('reviews',reviews);$('#v-title').value='';$('#v-notes').value='';renderReviews()}
function delReview(i){reviews.splice(i,1);save('reviews',reviews);renderReviews()}
const dancePlan={'周一':'基础律动 & 上身 Isolation','周二':'脚步组合 & 踩拍变化','周三':'休息/拉伸 + 复盘旧舞段','周四':'核心力量 + 框架定型','周五':'Freestyle 跟练 15 分钟','周六':'完整舞段学习','周日':'拍摄练习视频并对比'};
function genDancePlan(){
const el=$('#dance-plan');el.innerHTML='<ul class="list">'+Object.entries(dancePlan).map(([d,t])=>`<li><div class="text"><b>${d}</b> · ${t}</div></li>`).join('')+'</ul>';
}
function renderDanceNotes(){
const l=$('#dance-notes');if(!l)return;l.innerHTML='';
danceNotes.forEach((n,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text">${n}</div><button class="btn btn-danger" onclick="delDanceNote(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addDanceNote(){const v=$('#dance-note-input').value.trim();if(!v)return;danceNotes.unshift(v);save('danceNotes',danceNotes);$('#dance-note-input').value='';renderDanceNotes()}
function delDanceNote(i){danceNotes.splice(i,1);save('danceNotes',danceNotes);renderDanceNotes()}
const guitarBasics=['调音：标准 tuning E-A-D-G-B-e','右手拨弦：交替拨弦/下拨稳定，注意护弦','左手按弦：指尖垂直按弦，靠近品柱','爬格子：每日 5 分钟，从 60 bpm 开始','强力和弦：5 弦根与 6 弦根两种手型','推弦：无名指+中指辅助，保持音准'];
function showGuitarBasics(){$('#guitar-basics').innerHTML='<ul class="list">'+guitarBasics.map(b=>`<li><div class="text">${b}</div></li>`).join('')+'</ul>'}
function renderSongs(){
const l=$('#guitar-songs');if(!l)return;l.innerHTML='';
songs.forEach((s,i)=>{
const cls=s.status==='已会'?'b-green':(s.status==='复习'?'b-amber':'b-blue');
const li=document.createElement('li');
li.innerHTML=`<div class="text">${s.name} <span class="badge ${cls}">${s.status}</span></div><button class="btn btn-danger" onclick="delSong(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addSong(){const n=$('#g-song').value.trim(),s=$('#g-status').value;if(!n)return;songs.unshift({name:n,status:s});save('songs',songs);$('#g-song').value='';renderSongs()}
function delSong(i){songs.splice(i,1);save('songs',songs);renderSongs()}
function renderGears(){
const l=$('#gear-notes');if(!l)return;l.innerHTML='';
gears.forEach((g,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text">${g}</div><button class="btn btn-danger" onclick="delGear(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addGear(){const v=$('#g-gear').value.trim();if(!v)return;gears.unshift(v);save('gears',gears);$('#g-gear').value='';renderGears()}
function delGear(i){gears.splice(i,1);save('gears',gears);renderGears()}
const posturePlan={'高低肩':'靠墙站立 3 分钟 + 单肩拉伸各 30 秒 × 3 组','腰酸':'猫牛式 10 次 + 臀桥 12 次 × 3 组 + 髂腰肌拉伸'};
function genPosturePlan(){
const el=$('#posture-plan');el.innerHTML=Object.entries(posturePlan).map(([k,v])=>`<div class="idea"><div class="t">${k}</div><div class="m">${v}</div></div>`).join('');
}
function renderPostureLogs(){
const l=$('#posture-logs');if(!l)return;l.innerHTML='';
postureLogs.forEach((d,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text">✅ ${d}</div><button class="btn btn-danger" onclick="delPostureLog(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addPostureLog(){postureLogs.unshift(today()+' 完成体态矫正训练');save('postureLogs',postureLogs);renderPostureLogs()}
function delPostureLog(i){postureLogs.splice(i,1);save('postureLogs',postureLogs);renderPostureLogs()}
let finLogs=load('finLogs',[]);
const finData={
'stock':{title:'股票基础知识',tasks:[
{a:'什么是股票',d:'了解股票本质：买股票就是买公司的一部分',k:'什么是股票 股票入门基础'},
{a:'K线图怎么看',d:'学会看日线/周线K线图，理解阳线阴线',k:'K线图怎么看 股票K线入门'},
{a:'市盈率PE',d:'理解PE含义，判断股票贵不贵',k:'市盈率PE 股票估值指标'},
{a:'市净率PB',d:'理解PB含义，判断股价是否合理',k:'市净率PB 股票估值'},
{a:'成交量与换手率',d:'理解成交量和换手率的含义',k:'成交量换手率 股票基础'},
{a:'涨停跌停规则',d:'了解A股涨跌停板制度',k:'涨停跌停 A股交易规则'},
{a:'分红与除权除息',d:'理解股票分红和除权除息',k:'股票分红 除权除息'},
{a:'技术分析入门',d:'均线/MACD/KDJ基础指标',k:'股票技术分析 均线MACD入门'}
]},
'fund':{title:'基金理财知识',tasks:[
{a:'什么是基金',d:'了解基金本质：把钱交给专业人打理',k:'什么是基金 基金入门基础'},
{a:'指数基金定投',d:'学会定投沪深300/中证500',k:'指数基金定投 入门'},
{a:'ETF基金',d:'理解ETF和场内基金的区别',k:'ETF基金 场内基金'},
{a:'债券基金',d:'了解债基的风险和收益',k:'债券基金 稳健理财'},
{a:'基金费率',d:'理解申购费/赎回费/管理费',k:'基金费率 申购费赎回费'},
{a:'复利的力量',d:'理解复利，学会长期投资',k:'复利的力量 长期投资'}
]},
'economy':{title:'宏观经济知识',tasks:[
{a:'GDP是什么',d:'理解GDP含义和经济增长',k:'GDP是什么 宏观经济'},
{a:'CPI和通胀',d:'理解通胀对钱包的影响',k:'CPI通胀率 物价上涨'},
{a:'降息降准',d:'央行降息降准对股市的影响',k:'降息降准 央行货币政策'},
{a:'LPR利率',d:'理解LPR对房贷和投资的影响',k:'LPR利率 房贷利率'},
{a:'汇率与外汇',d:'人民币升值贬值的影响',k:'汇率外汇 人民币升值'}
]},
'skill':{title:'实战投资技巧',tasks:[
{a:'如何选股',d:'从行业到公司的选股思路',k:'如何选股 新手选股技巧'},
{a:'止损止盈',d:'学会设置止损和止盈点',k:'止损止盈 股票交易纪律'},
{a:'仓位管理',d:'不要满仓，学会分批建仓',k:'仓位管理 股票资金管理'},
{a:'财报怎么看',d:'看懂三大报表：资产负债/利润/现金流',k:'财报怎么看 三大报表入门'},
{a:'逆向投资',d:'别人恐惧我贪婪的逆向思维',k:'逆向投资 巴菲特投资理念'},
{a:'定投策略',d:'微笑曲线定投法',k:'基金定投策略 微笑曲线'}
]}
};
function genFinPlan(type){
const plan=finData[type];
const el=$('#fin-plan');
if(!plan){el.innerHTML='<div class="empty">暂无该类知识</div>';return}
el.innerHTML=`<div style="margin-bottom:8px;font-size:13px;color:#888">💰 <b style="color:#5e7ce0">${plan.title}</b></div>`+
plan.tasks.map((t,i)=>{
const bilibili=`https://search.bilibili.com/all?keyword=${encodeURIComponent(t.k)}`;
const douyin=`https://www.douyin.com/search/${encodeURIComponent(t.k)}`;
return `<div class="idea"><div class="t">${i+1}. ${t.a}</div><div class="m">📝 ${t.d}</div><div class="actions"><a href="${bilibili}" target="_blank" class="btn btn-primary" style="font-size:12px;padding:6px 10px">▶ B站教学</a><a href="${douyin}" target="_blank" class="btn btn-soft" style="font-size:12px;padding:6px 10px">▶ 抖音教学</a></div></div>`;
}).join('');
}
function renderFinVideos(){
const el=$('#fin-videos');if(!el)return;
const links=[{name:'股票入门全套',kw:'股票入门全套教程 新手'},{name:'基金定投教程',kw:'基金定投教程 新手'},{name:'财经新闻怎么看',kw:'财经新闻怎么看 入门'},{name:'巴菲特投资理念',kw:'巴菲特投资理念 入门'},{name:'A股交易规则',kw:'A股交易规则 新手必看'}];
el.innerHTML=links.map(l=>{
const b=`https://search.bilibili.com/all?keyword=${encodeURIComponent(l.kw)}`;
return `<div class="idea"><div class="t">${l.name}</div><div class="actions"><a href="${b}" target="_blank" class="btn btn-primary" style="font-size:12px;padding:6px 10px">▶ B站搜索</a></div></div>`;
}).join('');
}
function renderFinLogs(){
const l=$('#fin-logs');if(!l)return;l.innerHTML='';
finLogs.forEach((f,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text">${f}</div><button class="btn btn-danger" onclick="delFinLog(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addFinLog(){const v=$('#fin-log-input').value.trim();if(!v)return;finLogs.unshift(v+' ('+today()+')');save('finLogs',finLogs);$('#fin-log-input').value='';renderFinLogs()}
function delFinLog(i){finLogs.splice(i,1);save('finLogs',finLogs);renderFinLogs()}
let fitLogs=load('fitLogs',[]);
const fitData={
'胸':[{a:'杠铃卧推',d:'4组×8-10次'},{a:'哑铃飞鸟',d:'3组×12次'},{a:'上斜哑铃推举',d:'3组×10次'},{a:'俯卧撑',d:'3组×15次'},{a:'双杠臂屈伸',d:'3组×10次'}],
'背':[{a:'引体向上',d:'4组×6-8次'},{a:'杠铃划船',d:'4组×8-10次'},{a:'高位下拉',d:'3组×12次'},{a:'哑铃单臂划船',d:'3组×10次'},{a:'直臂下压',d:'3组×12次'}],
'腿':[{a:'杠铃深蹲',d:'4组×8-10次'},{a:'腿举',d:'3组×12次'},{a:'罗马尼亚硬拉',d:'3组×10次'},{a:'保加利亚分腿蹲',d:'3组×10次'},{a:'小腿提踵',d:'3组×15次'}],
'肩':[{a:'哑铃推举',d:'4组×8-10次'},{a:'侧平举',d:'3组×12次'},{a:'前平举',d:'3组×12次'},{a:'俯身飞鸟',d:'3组×12次'},{a:'面拉',d:'3组×15次'}],
'核心':[{a:'平板支撑',d:'3组×60秒'},{a:'卷腹',d:'3组×20次'},{a:'俄罗斯转体',d:'3组×20次'},{a:'悬垂举腿',d:'3组×12次'},{a:'登山者',d:'3组×30秒'}],
'全身燃脂':[{a:'波比跳',d:'4组×10次'},{a:'开合跳',d:'3组×30秒'},{a:'高抬腿',d:'3组×30秒'},{a:'深蹲跳',d:'3组×15次'},{a:'登山者',d:'3组×30秒'}]
};
function genFitPlan(part){
const moves=fitData[part]||[];
const el=$('#fit-plan');
if(!moves.length){el.innerHTML='<div class="empty">暂无该部位计划</div>';return}
el.innerHTML=`<div style="margin-bottom:8px;font-size:13px;color:#888">部位：<b style="color:#4c6e4b">${part}</b> · 点击动作名看教学视频</div>`+
moves.map(m=>{
const search=m.a+' 教学 标准动作';
const bilibili=`https://search.bilibili.com/all?keyword=${encodeURIComponent(search)}`;
const douyin=`https://www.douyin.com/search/${encodeURIComponent(search)}`;
return `<div class="idea"><div class="t">${m.a}</div><div class="m">建议：${m.d}</div><div class="actions"><a href="${bilibili}" target="_blank" class="btn btn-primary" style="font-size:12px;padding:6px 10px">▶ B站视频</a><a href="${douyin}" target="_blank" class="btn btn-soft" style="font-size:12px;padding:6px 10px">▶ 抖音视频</a></div></div>`;
}).join('');
}
function renderFitLogs(){
const l=$('#fit-logs');if(!l)return;l.innerHTML='';
fitLogs.forEach((f,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text">${f}<br><small style="color:#888">${today()}</small></div><button class="btn btn-danger" onclick="delFitLog(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addFitLog(){const v=$('#fit-log-input').value.trim();if(!v)return;fitLogs.unshift(v+' ('+today()+')');save('fitLogs',fitLogs);$('#fit-log-input').value='';renderFitLogs()}
function delFitLog(i){fitLogs.splice(i,1);save('fitLogs',fitLogs);renderFitLogs()}
let engLogs=load('engLogs',[]);
const engData={
1:{title:'第1月·打基础（音标+跟读+短句）',tasks:[
{a:'音标矫正练习',d:'每天20分钟，跟读48个国际音标',k:'英语音标发音教学 口型示范'},
{a:'影子跟读入门',d:'每天15分钟，跟读慢速英语材料',k:'影子跟读 shadowing 英语入门'},
{a:'日常短句背诵',d:'每天背5句实用口语短句',k:'英语日常口语短句 背诵'},
{a:'连读弱读训练',d:'每天10分钟，练习连读和弱读',k:'英语连读弱读 发音规则'},
{a:'录音对比纠正',d:'录自己的发音，对比原音找差距',k:'英语发音自测 录音对比纠正'}
]},
2:{title:'第2月·练表达（框架+场景+自述）',tasks:[
{a:'口语表达框架',d:'学习"观点+原因+例子"表达法',k:'英语口语表达框架 逻辑说话'},
{a:'场景对话练习',d:'餐厅/购物/问路/工作场景对话',k:'英语场景对话 实用口语'},
{a:'自言自语练习',d:'每天用英语描述自己的日常5分钟',k:'英语自言自语练习 daily routine'},
{a:'影子跟读进阶',d:'跟读TED演讲或美剧片段',k:'TED演讲影子跟读 美剧学口语'},
{a:'话题即兴说',d:'随机抽话题，即兴说1-2分钟',k:'英语即兴演讲 话题练习'}
]},
3:{title:'第3月·实战（模拟对话+演讲+录音）',tasks:[
{a:'模拟对话实战',d:'找语伴或用AI对话练习',k:'英语口语模拟对话 AI对话练习'},
{a:'话题演讲训练',d:'每周完成1个3分钟话题演讲',k:'英语演讲训练 3分钟演讲'},
{a:'影子跟读高级',d:'跟读新闻主播或快节奏对话',k:'英语新闻跟读 快速口语'},
{a:'录音对比复盘',d:'录完整对话，复盘语法和发音问题',k:'英语口语录音复盘 纠错'},
{a:'全英文日总结',d:'每天用英语总结一天，录下来',k:'英语日记 口头总结 daily summary'}
]}
};
function genEngPlan(month){
const plan=engData[month];
const el=$('#eng-plan');
if(!plan){el.innerHTML='<div class="empty">暂无该月计划</div>';return}
el.innerHTML=`<div style="margin-bottom:8px;font-size:13px;color:#888">📖 <b style="color:#5e7ce0">${plan.title}</b></div>`+
plan.tasks.map((t,i)=>{
const bilibili=`https://search.bilibili.com/all?keyword=${encodeURIComponent(t.k)}`;
const douyin=`https://www.douyin.com/search/${encodeURIComponent(t.k)}`;
return `<div class="idea"><div class="t">${i+1}. ${t.a}</div><div class="m">📝 ${t.d}</div><div class="actions"><a href="${bilibili}" target="_blank" style12px;padding:6px 10px">▶ B站教学</a><a href="${douyin}" target="_blank" class="btn btn-soft" style="font-size:12px;padding:6px 10px">▶ 抖音教学</a></div></div>`;
}).join('');
}
function renderEngVideos(){
const el=$('#eng-videos');if(!el)return;
const links=[{name:'影子跟读合集',kw:'影子跟读 英语口语 合集'},{name:'口语表达技巧',kw:'英语口语表达技巧 干货'},{name:'日常对话练习',kw:'英语日常对话练习 场景'},{name:'美剧学口语',kw:'美剧学口语 经典片段'},{name:'音标发音全套',kw:'英语音标发音教学 全套'}];
el.innerHTML=links.map(l=>{
const b=`https://search.bilibili.com/all?keyword=${encodeURIComponent(l.kw)}`;
return `<div class="idea"><div class="t">${l.name}</div><div class="actions"><a href="${b}" target="_blank" class="btn btn-primary" style="font-size:12px;padding:6px 10px">▶ B站搜索</a></div></div>`;
}).join('');
}
function renderEngLogs(){
const l=$('#eng-logs');if(!l)return;l.innerHTML='';
engLogs.forEach((e,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text">${e}</div><button class="btn btn-danger" onclick="delEngLog(${i})" style="padding:4px 
let opLogs=load('opLogs',[]);
const opData={
1:{title:'阶段1·建立运营认知（1-2周）',tasks:[
{a:'什么是运营',d:'了解运营的本质：连接产品和用户',k:'什么是运营 运营入门基础概念'},
{a:'运营岗位分类',d:'内容运营/用户运营/活动运营/新媒体运营',k:'运营岗位分类 区别 新媒体运营'},
{a:'运营核心思维',d:'用户思维+数据思维+迭代思维',k:'运营思维 用户思维 数据思维'},
{a:'常见运营指标',d:'DAU/留存率/转化率/ROI等',k:'运营指标 DAU 留存率 转化率'},
{a:'运营工具大全',d:'熟悉排版/做图/数据/剪辑工具',k:'运营工具推荐 新媒体工具大全'}
]},
2:{title:'阶段2·实操练习（3-6周）',tasks:[
{a:'小红书运营实操',d:'选题+标题+封面+正文排版',k:'小红书运营 0基础 选题技巧'},
{a:'抖音账号运营',d:'账号定位+内容规划+发布节奏',k:'抖音运营 0基础 账号定位'},
{a:'公众号运营',d:'选题策划+排版+涨粉技巧',k:'公众号运营 新手 选题涨粉'},
{a:'文案写作训练',d:'爆款标题+正文结构+钩子设计',k:'运营文案写作 爆款标题技巧'},
{a:'数据分析入门',d:'看懂后台数据+做复盘报告',k:'运营数据分析 后台数据 复盘'}
]},
3:{title:'阶段3·进阶提升（7-10周）',tasks:[
{a:'活动策划实战',d:'完整策划一场线上活动',k:'运营活动策划 线上活动方案'},
{a:'用户增长策略',d:'裂变/拉新/留存/召回方法',k:'用户增长 裂变拉新 留存召回'},
{a:'社群运营',d:'建群/活跃/转化/管理',k:'社群运营 建群活跃转化'},
{a:'投放与推广',d:'投流基础+ROI计算+优化',k:'运营投放 信息流投流 ROI'},
{a:'运营复盘报告',d:'写一份完整运营复盘报告',k:'运营复盘报告 模板 写法'}
]}
};
function genOpPlan(stage){
const plan=opData[stage];
const el=$('#op-plan');
if(!plan){el.innerHTML='<div class="empty">暂无该阶段计划</div>';return}
el.innerHTML=`<div style="margin-bottom:8px;font-size:13px;color:#888">📖 <b style="color:#ff9c19">${plan.title}</b></div>`+
plan.tasks.map((t,i)=>{
const bilibili=`https://search.bilibili.com/all?keyword=${encodeURIComponent(t.k)}`;
const douyin=`https://www.douyin.com/search/${encodeURIComponent(t.k)}`;
return `<div class="idea"><div class="t">${i+1}. ${t.a}</div><div class="m">📝 ${t.d}</div><div class="actions"><a href="${bilibili}" target="_blank" class="btn btn-primary" style="font-size:12px;padding:6px 10px">▶ B站教学</a><a href="${douyin}" target="_blank" class="btn btn-soft" style="font-size:12px;padding:6px 10px">▶ 抖音教学</a></div></div>`;
}).join('');
}
function renderOpVideos(){
const el=$('#op-videos');if(!el)return;
const links=[{name:'运营入门全套教程',kw:'运营入门全套教程 0基础'},{name:'小红书运营干货',kw:'小红书运营干货 爆款'},{name:'抖音运营从0开始',kw:'抖音运营从0开始 账号'},{name:'文案写作技巧',kw:'运营文案写作技巧 爆款标题'},{name:'数据分析复盘',kw:'运营数据分析复盘 教程'}];
el.innerHTML=links.map(l=>{
const b=`https://search.bilibili.com/all?keyword=${encodeURIComponent(l.kw)}`;
return `<div class="idea"><div class="t">${l.name}</div><div class="actions"><a href="${b}" target="_blank" class="btn btn-primary" style="font-size:12px;padding:6px 10px">▶ B站搜索</a></div></div>`;
}).join('');
}
function renderOpLogs(){
const l=$('#op-logs');if(!l)return;l.innerHTML='';
opLogs.forEach((o,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text">${o}</div><button class="btn btn-danger" onclick="delOpLog(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addOpLog(){const v=$('#op-log-input').value.trim();if(!v)return;opLogs.unshift(v+' ('+today()+')');save('opLogs',opLogs);$('#op-log-input').value='';renderOpLogs()}
function delOpLog(i){opLogs.splice(i,1);save('opLogs',opLogs);renderOpLogs()}
let pfLogs=load('pfLogs',[]);
const pfData={
'face':{title:'大小脸/脸型不流畅',tasks:[
{a:'咬肌放松按摩',d:'每天早晚各5分钟，放松偏大侧咬肌',k:'咬肌放松按摩 大小脸矫正'},
{a:'面部肌肉平衡训练',d:'练习双侧均匀咀嚼+表情肌对称发力',k:'面部肌肉平衡训练 大小脸'},
{a:'下颌关节松动术',d:'轻柔活动下颌关节，改善关节偏移',k:'下颌关节松动 颞下颌关节训练'},
{a:'舌位矫正',d:'正确舌顶上颚，改善面部发育',k:'舌位矫正 mewing 正确舌位'}
],note:'大小脸多为长期单侧咀嚼/睡姿导致，骨骼型需咨询正颌医生'},
'spine':{title:'脊柱侧弯',tasks:[
{a:'施罗特体操',d:'每天30分钟，专业脊柱侧弯矫正体操',k:'施罗特体操 脊柱侧弯矫正'},
{a:'死虫子动作',d:'3组×15次，核心稳定改善脊柱',k:'死虫子动作 dead bug 核心训练'},
{a:'鸟狗式',d:'3组×12次/侧，增强脊柱稳定肌',k:'鸟狗式 bird dog 脊柱稳定'},
{a:'侧屈拉伸',d:'每天2组×30秒/侧，拉伸凹侧肌肉',k:'脊柱侧弯侧屈拉伸 矫正运动'},
{a:'靠墙站立矫正',d:'每天10分钟，靠墙校准脊柱中立位',k:'靠墙站立 脊柱中立位 矫正'}
],note:'Cobb角＞40度建议就医，轻度可通过体操改善'},
'leg':{title:'O型腿',tasks:[
{a:'夹球深蹲',d:'膝盖夹球做深蹲3组×15次',k:'O型腿矫正 夹球深蹲'},
{a:'内侧肌肉强化',d:'坐姿夹腿3组×15次，强化内收肌',k:'O型腿 内收肌训练 腿型矫正'},
{a:'靠墙夹膝/k:'O型腿靠墙夹膝 矫正训练'},
{a:'弹力带内收',d:'弹力带内收腿3组×12次/侧',k:'弹力带内收腿 O型腿矫正'},
{a:'拉伸外侧肌肉',d:'拉伸髂胫束+臀肌30秒/侧',k:'髂胫束拉伸 O型腿 腿型'}
],note:'骨骼型O型腿严重者需就医，功能性可通过训练改善'},
'foot':{title:'足弓塌陷',tasks:[
{a:'缩足运动',d:'每天3组×20次，激活足底肌肉',k:'缩足运动 short foot 足弓训练'},
{a:'毛巾抓取',d:'用脚趾抓毛巾3组×20次',k:'毛巾抓取 足弓塌陷 足底训练'},
{a:'弹力带足外翻',d:'3组×15次/脚，强化足弓支撑肌',k:'弹力带足外翻 足弓训练'},
{a:'单腿站立平衡',d:'每天3组×30秒/脚，提升足弓稳定性',k:'单腿站立平衡 足弓稳定'},
{a:'网球足底按摩',d:'每天5分钟/脚，放松足底筋膜',k:'网球足底按摩 足底筋膜放松'}
],note:'严重扁平足建议定制矫形鞋垫'},
'calf':{title:'小腿外翻',tasks:[
{a:'足弓激活训练',d:'缩足运动3组×20次，改善足弓',k:'小腿外翻 足弓激活 矫正'},
{a:'正确步态训练',d:'走路注意足尖朝前+足弓发力',k:'正确步态训练 小腿外翻'},
{a:'拉伸小腿外侧',d:'每天3组×30秒/侧',k:'小腿外侧拉伸 腿型矫正'},
{a:'弹力带内旋',d:'3组×15次/脚，改善胫骨外旋',k:'弹力带内旋 小腿外翻矫正'},
{a:'泡沫轴放松',d:'每天5分钟/腿，放松小腿外侧',k:'泡沫轴小腿外侧 放松'}
],note:'小腿外翻多伴随足弓塌陷，需一起矫正'},
'chin':{title:'下巴后缩',tasks:[
{a:'舌顶上颚训练',d:'每天随时练习，正确舌位刺激下巴发育',k:'下巴后缩 舌顶上颚 mewing'},
{a:'颈部前屈矫正',d:'收下巴训练3组×15次',k:'收下巴训练 颈部前屈矫正'},
{a:'咬合检查',d:'检查是否有深覆合/反颌，建议看正畸',k:'下巴后缩 正畸 咬合问题'},
{a:'下颌前伸训练',d:'轻柔前伸下颌3组×10次',k:'下颌前伸训练 下巴后缩'},
{a:'改善口呼吸',d:'改用鼻呼吸，避免下颌后缩加重',k:'口呼吸矫正 鼻呼吸 下巴后缩'}
],note:'下巴后缩可能是骨骼/咬合问题，建议看正畸科医生'}
};
function genPfPlan(type){
const plan=pfData[type];
const el=$('#pf-plan');
if(!plan){el.innerHTML='<div class="empty">暂无该问题方案</div>';return}
el.innerHTML=`<div style="margin-bottom:8px;font-size:13px;color:#888">🦴 <b style="color:#5e7ce0">${plan.title}</b></div>`+
plan.tasks.map((t,i)=>{
const bilibili=`https://search.bilibili.com/all?keyword=${encodeURIComponent(t.k)}`;
const douyin=`https://www.douyin.com/search/${encodeURIComponent(t.k)}`;
return `<div class="idea"><div class="t">${i+1}. ${t.a}</div><div class="m">📝 ${t.d}</div><div class="actions"><a href="${bilibili}" target="_blank" class="btn btn-primary" style="font-size:12px;padding:6px 10px">▶ B站教学</a><a href="${douyin}" target="_blank" class="btn btn-soft" style="font-size:12px;padding:6px 10px">▶ 抖音教学</a></div></div>`;
}).join('')+
`<div style="margin-top:8px;padding:8px 12px;background:#fff3e0;border-radius:8px;font-size:12px;color:#92400e">⚠️ ${plan.note}</div>`;
}
function renderPfLogs(){
const l=$('#pf-logs');if(!l)return;l.innerHTML='';
pfLogs.forEach((p,i)=>{
const li=document.createElement('li');
li.innerHTML=`<div class="text">${p}</div><button class="btn btn-danger" onclick="delPfLog(${i})" style="padding:4px 8px;font-size:12px">删</button>`;
l.appendChild(li);
});
}
function addPfLog(){const v=$('#pf-log-input').value.trim();if(!v)return;pfLogs.unshift(v+' ('+today()+')');save('pfLogs',pfLogs);$('#pf-log-input').value='';renderPfLogs()}
function delPfLog(i){pfLogs.splice(i,1);save('pfLogs',pfLogs);renderPfLogs()}
function goHome(){closeDetail()}
function goStats(){
$('#detail-title').textContent='数据统计';
const streak=load('streak',0);
const done=habits.filter(h=>h.done).length;
$('#detail-body').innerHTML=`<div class="card"><h3>📊 总览</h3><ul class="list">
<li><div class="text">连续打卡 <b>${streak}</b> 天</div></li>
<li><div class="text">今日完成 ${done}/${habits.length}</div></li>
<li><div class="text">选题灵感 ${topics.length} 条</div></li>
<li><div class="text">纠错笔记 ${danceNotes.length} 条</div></li>
<li><div class="text">练习曲目 ${songs.length} 首</div></li>
<li><div class="text">金融学习 ${finLogs.length} 条</div></li>
<li><div class="text">健身记录 ${fitLogs.length} 条</div></li>
<li><div class="text">口语打卡 ${engLogs.length} 条</div></li>
<li><div class="text">运营学习 ${opLogs.length} 条</div></li>
<li><div class="text">体态矫正 ${pfLogs.length} 条</div></li>
</ul></div>`;
$('#detail-view').classList.add('active');
}
function goSettings(){
$('#detail-title').textContent='设置';
$('#detail-body').innerHTML=`<div class="card"><h3>⚙️ 设置</h3><p style="color:#888;font-size:13px;line-height:1.6">将本应用添加到手机主屏幕，即可像 App 一样使用。</p><div style="margin-top:12px"><button class="btn btn-danger" style="width:100%" onclick="clearAll()">🗑️ 清空全部数据</button></div></div>`;
$('#detail-view').classList.add('active');
}
function clearAll(){if(confirm('确定清空所有数据？')){Object.keys(LS).filter(k=>k.startsWith('wb_')).forEach(k=>LS.removeItem(k));location.reload()}}
renderMenu();
</script>
</body>
</html>
